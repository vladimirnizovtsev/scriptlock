## What this changes

One or two sentences. Link the issue if there is one.

## Why

## How it was verified

- [ ] `npm run typecheck` passes
- [ ] `npm test` passes (unit and e2e)
- [ ] New behaviour has a test, or the reason it does not is stated here

## Contract

- [ ] Changes to identity, integrity, scope, diff semantics or the CLI surface are reflected in DESIGN.md and `src/types.ts`
- [ ] User-facing wording follows the README "Limits" section (no "ensures compliance", "PCI compliant" or "QSA validated")
- [ ] Commits are signed off (`git commit -s`, Developer Certificate of Origin)
